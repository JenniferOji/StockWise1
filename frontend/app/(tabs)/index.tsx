import React from 'react';
import IntroPage from '../pages/intro';
// Render the Intro page here so the starter welcome screen no longer appears.
export default function HomeScreen() {
  return <IntroPage />;
}

